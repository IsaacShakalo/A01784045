function esPalindromo(texto) {

    const textoInvertido = palabra.split("").reverse().join("");

    return textoSinEspacios === textoInvertido;
  }

  const palabra = "Anita lava la tina";
  console.log(`${palabra} es un palíndromo`); 
